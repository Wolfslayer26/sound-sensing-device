this is just an sample file for code creation.

The list of commands to push are:

git add sample
git commit -m "Add the sample file"
git push


The list of commands to pull are:

git branch
git checkout main
git fetch origin
git pull origin main
git pull
